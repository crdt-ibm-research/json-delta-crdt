"use strict";

// Properties of the document root object
const ALIVE = "_alive"; // property containing info whether a key is alive
const MAP = "_m"; // property containing info if the key points to a map
const ARRAY = "_a"; // property containing info if the key points to an array
const VALUE = "_v"; // property containing info if the key points to a value
const FIRST = "_first";
const SECOND = "_second";

module.exports = {
  ALIVE,
  MAP,
  ARRAY,
  VALUE,
  FIRST,
  SECOND,
};
